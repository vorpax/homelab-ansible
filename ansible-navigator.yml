---
ansible-navigator:
  enable-prompts: true
  logging:
    level: debug
    append: false
  playbook-artifact:
    enable: true
    replay: /.artifacts
    save-as: "./artifacts/{playbook_dir}/{playbook_name}-artifact-{time_stamp}.json"
